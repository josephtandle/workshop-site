import type { ReactNode } from 'react'

export const metadata = {
  title: 'Web Design Arsenal',
  description: 'Free professional web design toolkit with skills, libraries, and animations to build gorgeous sites fast.',
}

export default function WebDesignArsenalLayout({ children }: { children: ReactNode }) {
  return children
}
