import type { ReactNode } from 'react'

export const metadata = {
  title: 'Web Designer Agent Kit',
  description: 'Free Claude Code Web Designer agent kit with 22 curated design, motion, WebGL, SEO, and GEO skills.',
}

export default function WebDesignArsenalLayout({ children }: { children: ReactNode }) {
  return children
}
