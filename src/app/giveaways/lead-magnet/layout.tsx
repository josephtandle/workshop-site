import type { ReactNode } from 'react'

export const metadata = {
  title: 'Success Stories',
  description: 'Real stories from a VIP dinner: what 17 people had to un-learn about success.',
}

export default function LeadMagnetLayout({ children }: { children: ReactNode }) {
  return children
}
