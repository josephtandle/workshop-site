import type { ReactNode } from 'react'

export const metadata = {
  title: 'Client Launch Checklist',
  description: 'A realistic Session 3 lead magnet example that collects first name and email, then delivers a checklist.',
}

export default function ClientLaunchChecklistLayout({ children }: { children: ReactNode }) {
  return children
}
