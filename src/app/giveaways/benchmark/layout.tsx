import type { ReactNode } from 'react'

export const metadata = {
  title: 'Claude vs OpenAI Benchmark',
  description: 'Pricing and capability comparison: Claude Opus vs GPT-5 across real workloads.',
}

export default function BenchmarkLayout({ children }: { children: ReactNode }) {
  return children
}
