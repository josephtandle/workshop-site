import type { ReactNode } from 'react'

export const metadata = {
  description: 'Free downloads and resources for learning AI and automation.',
}

export default function LeadMagnetsLayout({ children }: { children: ReactNode }) {
  return children
}
