import type { ReactNode } from 'react'

export const metadata = {
  title: 'Ultimate CLAUDE.md',
  description: 'Complete CLAUDE.md template for AI operating systems, memory layers, and Claude Code automation.',
}

export default function UltimeClaudeMDLayout({ children }: { children: ReactNode }) {
  return children
}
