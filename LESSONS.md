# Giveaway exceptions

- `/giveaways/terminal-to-claude-desktop` is intentionally free to watch with no signup. The shared `GiveawayCopyEmailGate` exact-matches the path with and without its trailing slash at event handling and rendering, so client navigation cannot expose an email modal on this video.
